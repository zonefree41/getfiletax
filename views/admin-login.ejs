<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Admin Login</title>
    <style>
        body {
            font-family: Arial;
            background: #fafafa;
            text-align: center;
            padding: 50px;
        }

        form {
            background: #fff;
            padding: 20px;
            display: inline-block;
            border-radius: 10px;
        }

        input {
            margin: 10px;
            padding: 10px;
            width: 80%;
        }

        button {
            background: #007bff;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
        }
    </style>
</head>

<body>
    <h2>Admin Login</h2>
    <form method="POST" action="/admin/mark-complete/<%= u.id %>">
        <input type="text" name="username" placeholder="Username" required>
        <input type="password" name="password" placeholder="Password" required>
        <button type="submit">Sign In</button>
    </form>
</body>

</html>
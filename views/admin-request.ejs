<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Client Requests</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            padding: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th,
        td {
            padding: 10px;
            border: 1px solid #ccc;
            text-align: left;
        }

        th {
            background: #f4f4f4;
        }
    </style>
</head>

<body>
    <h2>📋 Client Tax Requests</h2>
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Email</th>
                <th>Phone</th>
                <th>Type</th>
                <th>Message</th>
                <th>Date</th>
            </tr>
        </thead>
        <tbody>
            <% if (requests.length> 0) { %>
                <% requests.forEach(r=> { %>
                    <tr>
                        <td>
                            <%= r.name %>
                        </td>
                        <td>
                            <%= r.email %>
                        </td>
                        <td>
                            <%= r.phone %>
                        </td>
                        <td>
                            <%= r.filingType %>
                        </td>
                        <td>
                            <%= r.message %>
                        </td>
                        <td>
                            <%= r.date %>
                        </td>
                    </tr>
                    <% }) %>
                        <% } else { %>
                            <tr>
                                <td colspan="6">No requests yet.</td>
                            </tr>
                            <% } %>
        </tbody>
    </table>
</body>

</html>
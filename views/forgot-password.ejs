<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password | Tax-Prep</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;600&display=swap" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #007bff, #00c6ff);
            font-family: 'Poppins', sans-serif;
            color: #333;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .forgot-container {
            background: #fff;
            border-radius: 15px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            padding: 40px;
            width: 100%;
            max-width: 400px;
            text-align: center;
        }

        h2 {
            color: #007bff;
            font-weight: 600;
            margin-bottom: 20px;
        }

        p {
            font-size: 15px;
            color: #555;
        }

        .form-control {
            border-radius: 8px;
            padding: 12px;
            margin-bottom: 20px;
        }

        .btn-custom {
            background: linear-gradient(135deg, #ff9800, #ffc107);
            color: white;
            font-weight: bold;
            padding: 10px 20px;
            border: none;
            border-radius: 8px;
            transition: 0.3s;
        }

        .btn-custom:hover {
            background: linear-gradient(135deg, #e68900, #ffb300);
            transform: translateY(-2px);
        }

        a.back-home {
            display: block;
            margin-top: 15px;
            color: #007bff;
            text-decoration: none;
            font-size: 14px;
        }

        a.back-home:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>

    <div class="forgot-container">
        <h2>Forgot Password</h2>
        <p>Enter your email below and we’ll send you a password reset link.</p>
        <form action="/forgot-password" method="POST">
            <input type="email" name="email" class="form-control" placeholder="Enter your email" required>
            <button type="submit" class="btn-custom w-100">Send Reset Link</button>
        </form>
        <a href="/login" class="back-home">← Back to Login</a>
    </div>

</body>

</html>